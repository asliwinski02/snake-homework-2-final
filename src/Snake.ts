import display from "./display";

// place your code on

public class Snake {}
private String currentPosition: position;
private String currentDirection: direction;

move() {
if Snake= "Facing Foward"
 currentPosition="Increase its position by the given number"
 else "Decrease its position by the given number"

 this.position=new Point(this.position.x-1,this.position.y);
 this.position=new Point(this.position.x,this.potion.y+1);
 this.position=new Point(this.position.x+1,this.position.y);
 this.position=new Point(this.position.x,this.position.y-1);


}
turn () {
  if Snake="Facing foward"
  currentDirection="Turn it backward"
  else"Turn it foward"
}
public get position() {
  return currentPosition

  location() {
  newpoint= "x": 0
  newpoint="y": 0

  turnLeft() {
    if Snake="Facing left"
   currentDirection=" Move left"
    if Snake" Facing right"
    currentDirection="Move right"
    if Snake="Facing up"
    currentDirection="Move up"
    if Snake="Facing down"
    currentDirection="Move down"

  }
  turnRight() {
    if Snake="Facing left"
   currentDirection=" Move left"
    if Snake" Facing right"
    currentDirection="Move right"
    if Snake="Facing up"
    currentDirection="Move up"
    if Snake="Facing down"
    currentDirection="Move down"
  }
  worldModel() {
update(steps)="Snake's moves"
  }

  
}

public class Point2D {}
 public String xcoord()
 public String ycoord()

export default Snake;
